[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[phases.deploy]
cmd = 'npm run preview'

[variables]
NODE_ENV = 'production'
NPM_CONFIG_PRODUCTION = 'false'